(library (filters)
  (export character-replacement
          latex-figures
          lists)
  (import (rnrs) (wetd))

;;; TODO
  (define character-replacement '())
  (define latex-figures '())
  (define lists '()))
